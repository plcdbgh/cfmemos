# ========================================
# Cloudflare-Memos 后端 API 服务配置
# ========================================

name = "memos-api"
main = "src/index.js"
compatibility_date = "2024-01-15"
compatibility_flags = ["nodejs_compat"]

[vars]
# 时区配置（IANA 时区数据库格式）
# 示例: "Asia/Shanghai", "America/New_York", "Europe/London"
# 留空则使用 UTC
TIMEZONE = "Asia/Shanghai"

# ========================================
# 数据库配置
# ========================================
[[d1_databases]]
binding = "DB"
database_name = "cfmemos"
database_id = "723c2149-92e7-44f3-9cc1-675e7aec4ad3"

# ========================================
# 对象存储配置
# ========================================
[[r2_buckets]]
binding = "BUCKET"
bucket_name = "cfmemos"
